# Validation Spine — PR-time scope-mode run for TraigentSchema.
#
# Mirrors Traigent SDK's validation-spine-pr.yml. Findings are advisory during
# calibration; infrastructure/configuration failures fail closed so a required
# workflow cannot silently go green.
#
# Schema specializations:
#   - Path filter targets the schema package (traigent_schema/) plus
#     OpenAPI/JSON schema artifacts and tests/.
#   - Particularly relevant for the `assert_in_production` and
#     `weak_crypto_constant` markers (Schema #29/#33 calibration cases
#     came from this repo).
#   - Branches: main + develop (standard Schema PR targets).

name: validation-spine-pr

on:
  pull_request:
    branches: [main, develop]
    paths:
      - "traigent_schema/**"
      - "tests/**"
      - "pyproject.toml"
      - ".github/workflows/validation-spine-pr.yml"
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: validation-spine-pr-${{ github.ref }}
  cancel-in-progress: true

jobs:
  spine-pr-scan:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      SPINE_REF: 7766f10589b24971696c63d0235763a743cd9cd3  # pragma: allowlist secret
      SPINE_FILE_LIMIT: "5000"
      # Prefer a dedicated read-only spine token; fall back to this repo's
      # existing validation-CI PAT (used by validation-g8-dispatch.yml) so the
      # scan runs instead of failing closed when SPINE_RO_TOKEN is unset.
      SPINE_RO_TOKEN: ${{ secrets.SPINE_RO_TOKEN || secrets.VALIDATION_CI_PAT }}

    steps:
      # Spine's hardcoded `_PYTHON_REPOS` expects Schema sources at
      # `<workspace>/TraigentSchema/`, so check out at that subpath.
      - name: Checkout product repo
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
          path: TraigentSchema

      # continue-on-error here does NOT mean "tolerate a broken scan". It exists only so
      # this workflow can tell "token absent" apart from "token present but unusable"
      # (expired, revoked, or scoped without Contents:Read on the spine repo) and report
      # the precise reason before failing the job. Either way the job still fails closed
      # below: a scan that cannot run must not report success, because that is
      # indistinguishable from a clean scan. `spine-pr-scan` is advisory (required checks
      # on this repo are "Aikido Security: check code" and "ci-required"), so this failing
      # does not block merges — but it must still show red, honestly, not green.
      - name: Checkout validation spine
        id: spine-checkout
        if: env.SPINE_RO_TOKEN != ''
        continue-on-error: true
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: Traigent/traigent-validation-spine
          ref: ${{ env.SPINE_REF }}
          path: .spine
          token: ${{ env.SPINE_RO_TOKEN }}
          persist-credentials: false

      # Availability is proven by the checkout actually succeeding, not by the token merely
      # being non-empty. Every downstream step keys off SPINE_AVAILABLE.
      - name: Determine spine availability
        id: spine-available
        run: |
          if [ "${{ steps.spine-checkout.outcome }}" = "success" ] && [ -d "$GITHUB_WORKSPACE/.spine" ]; then
            echo "SPINE_AVAILABLE=true" >> "$GITHUB_ENV"
          else
            echo "SPINE_AVAILABLE=false" >> "$GITHUB_ENV"
          fi

      # Fail closed. A scan that cannot run must not report success — that would be
      # indistinguishable from a clean scan and defeats the point of running one.
      # Dependabot is exempt because GitHub withholds repository secrets from dependabot
      # PRs by design, and dependency bumps carry no policy surface.
      - name: Fail closed when spine scan cannot run
        if: env.SPINE_AVAILABLE != 'true'
        run: |
          if [ "${{ github.actor }}" = "dependabot[bot]" ]; then
            echo "::notice::Spine scan skipped for dependabot PR (secrets blocked by design); dependabot bumps carry no policy surface."
            exit 0
          fi
          if [ -z "${SPINE_RO_TOKEN}" ]; then
            reason="repository secret SPINE_RO_TOKEN is not configured (and no VALIDATION_CI_PAT fallback exists)"
          else
            reason="the configured token could not check out Traigent/traigent-validation-spine (expired, revoked, or missing Contents:Read)"
          fi
          echo "::error::Validation spine scan FAILED TO RUN — ${reason}. Failing closed: a scan that cannot execute must not report success."
          {
            echo "## Validation Spine — PR scan: FAILED TO RUN"
            echo
            echo "**No gap report was produced for this PR.** Absence of findings below is not evidence of none."
            echo
            echo "Reason: ${reason}."
            echo
            echo "Fix: rotate the read-only spine token with"
            echo '`tools/secret-ops/rotate-spine-ro-token.sh` in the traigent-workspace repo'
            echo "(owner-run; sets \`SPINE_RO_TOKEN\` across all repos running this workflow)."
          } >> "$GITHUB_STEP_SUMMARY"
          exit 1

      - name: Capture PR changed files
        if: env.SPINE_AVAILABLE == 'true' && github.event_name == 'pull_request'
        working-directory: TraigentSchema
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$GITHUB_WORKSPACE/.spine-health"
          git diff --name-only --diff-filter=ACMRT \
            "${{ github.event.pull_request.base.sha }}...${{ github.event.pull_request.head.sha }}" \
            -- 'traigent_schema/**' 'tests/**' 'pyproject.toml' '.github/workflows/validation-spine-pr.yml' \
            > "$GITHUB_WORKSPACE/.spine-health/pr-changed-files.txt"
          count="$(wc -l < "$GITHUB_WORKSPACE/.spine-health/pr-changed-files.txt" | tr -d ' ')"
          echo "Captured ${count} PR changed file(s) for scoped summary."

      - name: Set up Python 3.11
        if: env.SPINE_AVAILABLE == 'true'
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.11"

      - name: Install uv
        if: env.SPINE_AVAILABLE == 'true'
        run: |
          pip install --upgrade --only-binary :all: pip "uv==0.8.13"

      - name: Install spine
        if: env.SPINE_AVAILABLE == 'true'
        working-directory: .spine
        run: uv sync --extra dev

      - name: Run security_ledger against product repo
        if: env.SPINE_AVAILABLE == 'true'
        id: spine-run
        continue-on-error: true
        working-directory: .spine
        run: |
          mkdir -p "$GITHUB_WORKSPACE/.spine-health"
          # The scanner still runs over the repository because the pinned
          # security-ledger CLI has no safe changed-file include switch here.
          # The summary below separates PR-touched findings from existing
          # repository-wide noise.
          ./.venv/bin/validation security-ledger run \
            --workspace-root "$GITHUB_WORKSPACE" \
            --health-dir "$GITHUB_WORKSPACE/.spine-health" \
            --file-limit "$SPINE_FILE_LIMIT"

      # A scan that examined zero files is not a clean scan. The spine's
      # security_ledger only knows the repos listed in `_PYTHON_REPOS`
      # (Traigent, TraigentBackend, TraigentSchema). If this repo ever drops
      # out of that list, or the checkout subpath drifts, the report comes
      # back with `file_count: 0` and every gate below passes vacuously —
      # exactly how TraigentFrontend and traigent-js scanned nothing while
      # reporting success. Assert the scan had subjects.
      - name: Assert the scan examined files
        if: env.SPINE_AVAILABLE == 'true'
        run: |
          set -euo pipefail
          graph="$GITHUB_WORKSPACE/.spine-health/latest-security-graph.json"
          if [ ! -f "$graph" ]; then
            echo "::error::no security graph emitted; the scan did not run."
            exit 1
          fi
          files="$(python3 -c "import json,sys; print(json.load(open(sys.argv[1]))['scope_summary']['file_count'])" "$graph")"
          echo "spine scan examined $files files"
          if [ "$files" -eq 0 ]; then
            echo "::error::spine scan examined 0 files — this repo is not covered by the scanner (see _PYTHON_REPOS in the spine's security_ledger/graph.py). A zero-subject scan cannot pass."
            exit 1
          fi

      - name: Summarize new critical findings
        if: always() && env.SPINE_AVAILABLE == 'true'
        working-directory: .spine
        run: |
          set -e
          GAPS="$GITHUB_WORKSPACE/.spine-health/latest-security-ledger-gaps.json"
          if [[ ! -f "$GAPS" ]]; then
            echo "::error::no gap report emitted by validation spine scan"
            exit 1
          fi
          ./.venv/bin/python - <<'PY'
          import json, os, pathlib
          gaps_path = pathlib.Path(os.environ["GITHUB_WORKSPACE"]) / ".spine-health" / "latest-security-ledger-gaps.json"
          changed_path = pathlib.Path(os.environ["GITHUB_WORKSPACE"]) / ".spine-health" / "pr-changed-files.txt"
          data = json.loads(gaps_path.read_text())
          by_severity = (data.get("summary") or {}).get("by_severity") or {}
          critical = by_severity.get("critical", 0)
          high     = by_severity.get("high", 0)
          medium   = by_severity.get("medium", 0)
          changed = {line.strip() for line in changed_path.read_text().splitlines()} if changed_path.exists() else set()

          def iter_gaps(payload):
              for key in ("gaps", "items", "blockers", "records", "findings"):
                  value = payload.get(key)
                  if isinstance(value, list):
                      return value
              return []

          def normalized_ref_path(gap):
              ref = str(gap.get("source_ref") or gap.get("path") or gap.get("file") or "")
              ref = ref.split("#", 1)[0]
              if ref.startswith("TraigentSchema/"):
                  ref = ref[len("TraigentSchema/"):]
              if ":" in ref:
                  ref = ref.rsplit(":", 1)[0]
              return ref

          scoped = {"critical": 0, "high": 0, "medium": 0}
          gap_items = iter_gaps(data)
          if changed and gap_items:
              for gap in gap_items:
                  ref = normalized_ref_path(gap)
                  if ref in changed:
                      severity = str(gap.get("severity") or "").lower()
                      if severity in scoped:
                          scoped[severity] += 1
          step_summary = pathlib.Path(os.environ["GITHUB_STEP_SUMMARY"])
          with step_summary.open("a") as fh:
              fh.write("## Validation Spine — PR scan\n\n")
              fh.write("| Severity | Repo-wide count | PR-touched count |\n| --- | ---: | ---: |\n")
              fh.write(f"| critical | {critical} | {scoped['critical']} |\n")
              fh.write(f"| high | {high} | {scoped['high']} |\n")
              fh.write(f"| medium | {medium} | {scoped['medium']} |\n\n")
              if changed and not gap_items:
                  fh.write("PR-scoped counts unavailable: scanner output did not expose a parseable finding list.\n\n")
              if critical:
                  fh.write("**Critical repo-wide findings — review before merge.** ")
                  fh.write("Full detail in the `validation-spine-gaps` artifact below.\n")
              else:
                  fh.write("No critical findings.\n")
          print(
              f"critical={critical} high={high} medium={medium} "
              f"pr_critical={scoped['critical']} pr_high={scoped['high']} pr_medium={scoped['medium']}"
          )
          if critical:
              print(f"::warning::spine reported {critical} critical finding(s); see artifact")
          PY

      - name: Upload spine gap artifacts
        if: always() && env.SPINE_AVAILABLE == 'true'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: validation-spine-gaps
          path: .spine-health/
          # `.spine-health` is a dot-directory, and upload-artifact excludes
          # hidden paths by default — so this artifact has never actually
          # uploaded ("No files were found with the provided path"), and
          # `if-no-files-found: warn` kept that quiet. The findings the scan
          # produces are the only reviewable evidence it exists to produce.
          include-hidden-files: true
          if-no-files-found: error
          retention-days: 14
