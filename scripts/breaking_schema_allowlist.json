{
  "$comment": "Explicit, versioned opt-outs for scripts/breaking_schema_check.py (an ENFORCE, blocking gate). The gate's rule is not 'you may not break the contract' — it is 'you may not break it silently'. A change stays allowed; the silence does not. An entry only grants an opt-out when file, rule, reason, and version are all present, AND reason is real prose explaining WHY the tightening is intentional and reviewed — a bare issue/PR reference, 'TODO', 'see PR', or anything under ~20 chars / a single word is rejected (see reason_rejection()). pointer_prefix is optional and defaults to matching every finding for that rule in that file; add it to scope a narrower, single-field opt-out instead. Entries do not expire — a bound-hardening acknowledgement is a permanent historical fact about that contract, not a lease.",
  "entries": [
    {
      "file": "smartops_v2/shadow_evaluate_response_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/evidence_case",
      "reason": "EvidenceCase is an intentional coordinated response expansion. Pinning TraigentBackend to this Schema revision before planner emission is necessary but not sufficient: rollout remains blocked until every strict or generated response consumer is reconciled with evidence_case.",
      "version": "5.6.0",
      "pr": "#377"
    },
    {
      "file": "x_extensions_meta_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/x-traigent-invariants",
      "reason": "This intentionally expands the governed extension vocabulary so schemas can declare mechanically checked cross-field equality. It is contract mechanics only, and consumers of the closed registry must update for the new property.",
      "version": "5.6.0",
      "pr": "#377"
    },
    {
      "file": "x_extensions_meta_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/x-disclosure",
      "reason": "Registers the Agent Lifecycle Record Part 1 client-boundary disclosure annotation (PART1_CONTRACT_FREEZE_DRAFT.md:376-408) in the governed x-* extension vocabulary so agent_lifecycle_record/*.json schemas can carry it, matching the existing x-traigent-invariants precedent immediately above. This file is documentation/governance metadata only -- its own header states it is 'not applied as a JSON Schema validator over other schemas' -- so no real request or response payload is ever validated against x_extensions_meta_schema.json itself; the breaking-check finding is the generic conservative heuristic correctly erring cautious on a bare-named resource file, not evidence of an actual wire-consumer break. Unlocks contract mutation row 17 (PART1_CONTRACT_FREEZE_DRAFT.md:1155), one of the five Schema-owned mutations Internal Contract Freeze requires to be executable.",
      "version": "5.6.0",
      "pr": "pending (branch feature/agent-lifecycle-record-part1-receipts, WorkIntent st_1c36bc0e8e53)"
    },
    {
      "file": "execution/execution_endpoints.json",
      "rule": "property_added",
      "reason": "identity_state is an explicit discriminator when present, with a closed enum of identified/unidentified and a strictly validated absent/legacy migration branch (expand-contract); rollout stays flag-off until Backend and strict consumers emit and handle it.",
      "version": "5.6.0",
      "pr": "#400"
    },
    {
      "file": "execution/execution_endpoints.json",
      "rule": "type",
      "reason": "agent_id is nullable only in the unidentified branch, with paired-null invariants, because inventing an agent would corrupt provenance; identified rows remain string-valued.",
      "version": "5.6.0",
      "pr": "#400"
    },
    {
      "file": "execution/execution_endpoints.json",
      "rule": "required",
      "reason": "Expand-contract: identity_state is dropped from the top-level required list because the active Backend does not yet emit it on every experiment-group item or provenance row. The enum stays closed to identified/unidentified only; absence is handled by a new, strictly validated legacy branch (string agent_id, string-or-null dataset_id, no run_id), never treated as identified or as a certified explicit-none dataset claim. A follow-up MAJOR revision re-tightens this back to required once the Backend backfills the field everywhere.",
      "version": "5.6.0",
      "pr": "#400"
    },
    {
      "file": "execution/execution_endpoints.json",
      "rule": "allOf_branch_count_changed",
      "reason": "The allOf branch count on ExperimentGroupOverview goes from 3 to 4 and on GroupedConfigurationRunProvenance goes from 2 to 3 because a new, absence-only branch was added alongside the existing identified/unidentified branches to enforce the legacy no-identity_state shape structurally (string agent_id, string-or-null dataset_id, run_id null-or-absent). The pre-existing branches on each definition are unchanged; the new branch only tightens validation for the newly-optional legacy case, it does not loosen any previously-enforced constraint.",
      "version": "5.6.0",
      "pr": "#400"
    },
    {
      "file": "execution/experiment_group_schema.json",
      "rule": "required",
      "reason": "Expand-contract: identity_state is dropped from the top-level required list on ExperimentGroupOverview and GroupedConfigurationRunProvenance because the active Backend does not yet emit it on every item or row. The enum stays closed to identified/unidentified only; absence is handled by a new, strictly validated legacy branch (string agent_id, string-or-null dataset_id, no run_id), never treated as identified or as a certified explicit-none dataset claim. A follow-up MAJOR revision re-tightens this back to required once the Backend backfills the field everywhere.",
      "version": "5.6.0",
      "pr": "#400"
    },
    {
      "file": "execution/experiment_group_schema.json",
      "rule": "allOf_branch_count_changed",
      "reason": "The allOf branch count on ExperimentGroupOverview goes from 3 to 4 and on GroupedConfigurationRunProvenance goes from 2 to 3 because a new, absence-only branch was added alongside the existing identified/unidentified branches to enforce the legacy no-identity_state shape structurally (string agent_id, string-or-null dataset_id, run_id null-or-absent). The pre-existing branches on each definition are unchanged; the new branch only tightens validation for the newly-optional legacy case, it does not loosen any previously-enforced constraint.",
      "version": "5.6.0",
      "pr": "#400"
    },
    {
      "file": "analytics/curation_advice_schema.json",
      "rule": "enum",
      "pointer_prefix": "#/properties/advice/items/properties/category",
      "reason": "Retires the subtractive curation vocabulary at the client boundary (#389). `remove_redundant` is removed from the enum because acting on subtractive advice keyed to an observed signal biases the customer's own measurement and is unrepairable -- excluded items carry zero inclusion probability. Measured on a 70-item confirmation set, top-15-by-observed-discrimination pruning gave -0.202 against a true -0.073 (2.77x); random-15 was unbiased at -0.078. Breaking is the intended effect: a consumer still emitting or matching this member must be reconciled rather than silently accepted. Recorded in the disclosure register; reintroducing a subtractive category needs an owner decision. Scoped by pointer_prefix to the one enum that actually carried the retired member: an entry with no prefix defaults to matching every enum finding in the file forever (find_allow_entry uses startswith on an empty string), which would silently pre-authorize unrelated future enum breaks.",
      "version": "5.6.0",
      "pr": "#389"
    },
    {
      "file": "analytics/run_example_insights_schema.json",
      "rule": "enum",
      "pointer_prefix": "#/properties/example_rows/items/properties/recommended_action",
      "reason": "Retires the subtractive curation vocabulary at the client boundary (#389). `remove_redundant` is removed from the enum because acting on subtractive advice keyed to an observed signal biases the customer's own measurement and is unrepairable -- excluded items carry zero inclusion probability. Measured on a 70-item confirmation set, top-15-by-observed-discrimination pruning gave -0.202 against a true -0.073 (2.77x); random-15 was unbiased at -0.078. Breaking is the intended effect: a consumer still emitting or matching this member must be reconciled rather than silently accepted. Recorded in the disclosure register; reintroducing a subtractive category needs an owner decision. Scoped by pointer_prefix because this file has five enums and only recommended_action carried the retired member; without a prefix the entry would also pre-authorize breaking changes to dataset_quality, review_priority, difficulty_bucket and suspicious_flags.",
      "version": "5.6.0",
      "pr": "#389"
    },
    {
      "file": "agent_lifecycle_record/agent_lifecycle_record_response_schema.json",
      "rule": "type",
      "pointer_prefix": "#/properties/record_ref",
      "reason": "ALR-1106 (Agent Lifecycle Record Part 1, Route 1/2 packet): moves AgentLifecycleRecordRef out of this file's own local `definitions` block into agent_lifecycle_common_schema.json#/definitions/AgentLifecycleRecordRef so Route 1's new agent_lifecycle_agent_revision_register_response_schema.json and this Route 5 response share one definition instead of duplicating it -- the contract's exact-override manifest (PART1_CONTRACT_FREEZE_DRAFT.md:434-437) always named the common schema as this shape's eventual home; ALR-1105 could only leave it local because no Route 1 response schema existed yet on that branch. The wire shape is byte-for-byte unchanged (same type=object, same additionalProperties:false, same required=[kind,id,version_id], same three property schemas -- only the internal $ref target for id/version_id moved from a same-file OpaqueRefId pointer to the identical common-schema OpaqueRefId pointer both files already share). This finding is the breaking-schema-check's generic conservative heuristic correctly erring cautious on a cross-file $ref it does not dereference (record_ref now resolves via `allOf:[{\"$ref\":\"agent_lifecycle_common_schema.json#/definitions/AgentLifecycleRecordRef\"}]` instead of an in-file `#/definitions/AgentLifecycleRecordRef`), not evidence of an actual wire-consumer break -- no route has been implemented against this pre-freeze draft contract yet, so there is no live consumer to break. Scoped by pointer_prefix to record_ref only.",
      "version": "5.6.0",
      "pr": "pending (branch feature/agent-lifecycle-record-part1-routes12, WorkIntent st_92bfb968e8cb)"
    },
    {
      "file": "agent_lifecycle_record/agent_lifecycle_record_response_schema.json",
      "rule": "additionalProperties",
      "pointer_prefix": "#/properties/record_ref",
      "reason": "Same ALR-1106 AgentLifecycleRecordRef relocation described in the paired 'type' entry immediately above for this same file/pointer -- the breaking-schema-check does not dereference the new cross-file $ref, so it reads record_ref as having gone from additionalProperties:false to open, when the resolved shape (agent_lifecycle_common_schema.json#/definitions/AgentLifecycleRecordRef) is still additionalProperties:false, byte-for-byte identical to the local definition it replaced. Scoped by pointer_prefix to record_ref only.",
      "version": "5.6.0",
      "pr": "pending (branch feature/agent-lifecycle-record-part1-routes12, WorkIntent st_92bfb968e8cb)"
    },
    {
      "file": "agent_lifecycle_record/agent_lifecycle_record_response_schema.json",
      "rule": "required",
      "pointer_prefix": "#/properties/record_ref",
      "reason": "Same ALR-1106 AgentLifecycleRecordRef relocation described in the paired 'type' entry above for this same file/pointer -- the breaking-schema-check does not dereference the new cross-file $ref, so it reads record_ref's required=[kind,id,version_id] as dropped entirely, when the resolved shape (agent_lifecycle_common_schema.json#/definitions/AgentLifecycleRecordRef) still requires exactly those three fields, byte-for-byte identical to the local definition it replaced. Scoped by pointer_prefix to record_ref only.",
      "version": "5.6.0",
      "pr": "pending (branch feature/agent-lifecycle-record-part1-routes12, WorkIntent st_92bfb968e8cb)"
    },
    {
      "file": "auth/api_key_create_request_schema.json",
      "rule": "required",
      "pointer_prefix": "#",
      "reason": "Not a migration consumers must make: the OLD schema described an API that never existed. key_name becomes required because the live route already rejects a create without it. Verified against the live route on TraigentBackend origin/develop, src/routes/api_key_routes.py:548,571 -- the create schema declares key_name=fields.Str(required=True, Length(1,255)) and expires_in_days=fields.Int(Range(1,3650)); no 'name', no 'expires_at'.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/api_key_create_request_schema.json",
      "rule": "property_removed",
      "pointer_prefix": "#/properties/expires_at",
      "reason": "Not a migration consumers must make: the OLD schema described an API that never existed. 'expires_at' is removed because the route takes expires_in_days instead; a client sending it was already ignored. Verified against the live route on TraigentBackend origin/develop, src/routes/api_key_routes.py:548,571 -- the create schema declares key_name=fields.Str(required=True, Length(1,255)) and expires_in_days=fields.Int(Range(1,3650)); no 'name', no 'expires_at'.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/api_key_create_request_schema.json",
      "rule": "property_removed",
      "pointer_prefix": "#/properties/name",
      "reason": "Not a migration consumers must make: the OLD schema described an API that never existed. 'name' is removed because the route reads key_name; a client sending it was already ignored. Verified against the live route on TraigentBackend origin/develop, src/routes/api_key_routes.py:548,571 -- the create schema declares key_name=fields.Str(required=True, Length(1,255)) and expires_in_days=fields.Int(Range(1,3650)); no 'name', no 'expires_at'.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/api_key_create_request_schema.json",
      "rule": "type",
      "pointer_prefix": "#/properties/scopes",
      "reason": "Not a migration consumers must make: the OLD schema described an API that never existed. scopes becomes an array because that is what the route has always read. Verified against the live route on TraigentBackend origin/develop, src/routes/api_key_routes.py:341-358 -- scopes/permissions are read as list[str]; the route never parses a space-delimited string.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/api_key_create_request_schema.json",
      "rule": "minItems",
      "pointer_prefix": "#/properties/scopes",
      "reason": "Not a migration consumers must make: the OLD schema described an API that never existed. An empty scopes array is meaningless to the route's allowlist check, so minItems 1 documents existing behaviour. Verified against the live route on TraigentBackend origin/develop, src/routes/api_key_routes.py:341-358 -- scopes/permissions are read as list[str]; the route never parses a space-delimited string.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/api_key_create_request_schema.json",
      "rule": "items",
      "pointer_prefix": "#/properties/scopes/items",
      "reason": "Bounds each scope token (maxLength 64) to close the unbounded-request-string input-hardening gap enforced by tests/test_request_string_maxlength.py (#138). The longest value in the route's closed scope vocabulary is 'benchmarks:write' (16 chars), so 64 constrains input without excluding any real value.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/api_key_resource_schema.json",
      "rule": "required",
      "pointer_prefix": "#",
      "reason": "Not a migration consumers must make: the OLD schema described an API that never existed. The required set is realigned to the fields the route actually emits; 'name' and 'prefix' drop out because no response ever carried them. Verified against the live route on TraigentBackend origin/develop, src/routes/api_key_routes.py:1263-1282 -- the 201 body is {success, message, data{key, key_id, key_prefix, key_name, key_type, created_at}}}}.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/api_key_resource_schema.json",
      "rule": "property_removed",
      "pointer_prefix": "#/properties/create_secret",
      "reason": "Not a migration consumers must make: the OLD schema described an API that never existed. 'create_secret' is removed because it is a stale invention -- the real secret is data.key. Verified against the live route on TraigentBackend origin/develop, src/routes/api_key_routes.py:1263-1282 -- the 201 body is {success, message, data{key, key_id, key_prefix, key_name, key_type, created_at}}}}.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/api_key_resource_schema.json",
      "rule": "property_removed",
      "pointer_prefix": "#/properties/expires_at",
      "reason": "Not a migration consumers must make: the OLD schema described an API that never existed. 'expires_at' is removed because it is absent from the 201 body. Verified against the live route on TraigentBackend origin/develop, src/routes/api_key_routes.py:1263-1282 -- the 201 body is {success, message, data{key, key_id, key_prefix, key_name, key_type, created_at}}}}.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/api_key_resource_schema.json",
      "rule": "property_removed",
      "pointer_prefix": "#/properties/name",
      "reason": "Not a migration consumers must make: the OLD schema described an API that never existed. 'name' is removed because it is the response carries key_name. Verified against the live route on TraigentBackend origin/develop, src/routes/api_key_routes.py:1263-1282 -- the 201 body is {success, message, data{key, key_id, key_prefix, key_name, key_type, created_at}}}}.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/api_key_resource_schema.json",
      "rule": "property_removed",
      "pointer_prefix": "#/properties/prefix",
      "reason": "Not a migration consumers must make: the OLD schema described an API that never existed. 'prefix' is removed because it is the response carries key_prefix. Verified against the live route on TraigentBackend origin/develop, src/routes/api_key_routes.py:1263-1282 -- the 201 body is {success, message, data{key, key_id, key_prefix, key_name, key_type, created_at}}}}.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/api_key_resource_schema.json",
      "rule": "property_removed",
      "pointer_prefix": "#/properties/project_id",
      "reason": "Not a migration consumers must make: the OLD schema described an API that never existed. 'project_id' is removed because it is never emitted by this route. Verified against the live route on TraigentBackend origin/develop, src/routes/api_key_routes.py:1263-1282 -- the 201 body is {success, message, data{key, key_id, key_prefix, key_name, key_type, created_at}}}}.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/api_key_resource_schema.json",
      "rule": "property_removed",
      "pointer_prefix": "#/properties/scopes",
      "reason": "Not a migration consumers must make: the OLD schema described an API that never existed. 'scopes' is removed because it is not part of the create response. Verified against the live route on TraigentBackend origin/develop, src/routes/api_key_routes.py:1263-1282 -- the 201 body is {success, message, data{key, key_id, key_prefix, key_name, key_type, created_at}}}}.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/api_key_resource_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/key",
      "reason": "Not a migration consumers must make: the OLD schema described an API that never existed. 'key' is added because the route already emits it; the old closed additionalProperties made a truthful schema impossible. Verified against the live route on TraigentBackend origin/develop, src/routes/api_key_routes.py:1263-1282 -- the 201 body is {success, message, data{key, key_id, key_prefix, key_name, key_type, created_at}}}}.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/api_key_resource_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/key_name",
      "reason": "Not a migration consumers must make: the OLD schema described an API that never existed. 'key_name' is added because the route already emits it; the old closed additionalProperties made a truthful schema impossible. Verified against the live route on TraigentBackend origin/develop, src/routes/api_key_routes.py:1263-1282 -- the 201 body is {success, message, data{key, key_id, key_prefix, key_name, key_type, created_at}}}}.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/api_key_resource_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/key_prefix",
      "reason": "Not a migration consumers must make: the OLD schema described an API that never existed. 'key_prefix' is added because the route already emits it; the old closed additionalProperties made a truthful schema impossible. Verified against the live route on TraigentBackend origin/develop, src/routes/api_key_routes.py:1263-1282 -- the 201 body is {success, message, data{key, key_id, key_prefix, key_name, key_type, created_at}}}}.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/api_key_resource_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/key_type",
      "reason": "Not a migration consumers must make: the OLD schema described an API that never existed. 'key_type' is added because the route already emits it; the old closed additionalProperties made a truthful schema impossible. Verified against the live route on TraigentBackend origin/develop, src/routes/api_key_routes.py:1263-1282 -- the 201 body is {success, message, data{key, key_id, key_prefix, key_name, key_type, created_at}}}}.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/keys_endpoints.json",
      "rule": "required",
      "pointer_prefix": "#/paths//api/v1/keys/post/requestBody/content/application/json/schema",
      "reason": "Not a migration consumers must make: the OLD schema described an API that never existed. Mirrors the create-request realignment onto the POST /api/v1/keys catalogue entry. Verified against the live route on TraigentBackend origin/develop, src/routes/api_key_routes.py:548,571 -- the create schema declares key_name=fields.Str(required=True, Length(1,255)) and expires_in_days=fields.Int(Range(1,3650)); no 'name', no 'expires_at'.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/keys_endpoints.json",
      "rule": "required",
      "pointer_prefix": "#/paths//api/v1/keys/post/responses/201/content/application/json/schema",
      "reason": "Not a migration consumers must make: the OLD schema described an API that never existed. Mirrors the create-response realignment onto the POST /api/v1/keys catalogue entry. Verified against the live route on TraigentBackend origin/develop, src/routes/api_key_routes.py:1263-1282 -- the 201 body is {success, message, data{key, key_id, key_prefix, key_name, key_type, created_at}}}}.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/keys_endpoints.json",
      "rule": "property_removed",
      "pointer_prefix": "#/paths//api/v1/keys/post/requestBody/content/application/json/schema/properties/expires_at",
      "reason": "Not a migration consumers must make: the OLD schema described an API that never existed. Same 'expires_at' removal as the standalone create-request schema, kept in sync. Verified against the live route on TraigentBackend origin/develop, src/routes/api_key_routes.py:548,571 -- the create schema declares key_name=fields.Str(required=True, Length(1,255)) and expires_in_days=fields.Int(Range(1,3650)); no 'name', no 'expires_at'.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/keys_endpoints.json",
      "rule": "property_removed",
      "pointer_prefix": "#/paths//api/v1/keys/post/requestBody/content/application/json/schema/properties/name",
      "reason": "Not a migration consumers must make: the OLD schema described an API that never existed. Same 'name' removal as the standalone create-request schema, kept in sync. Verified against the live route on TraigentBackend origin/develop, src/routes/api_key_routes.py:548,571 -- the create schema declares key_name=fields.Str(required=True, Length(1,255)) and expires_in_days=fields.Int(Range(1,3650)); no 'name', no 'expires_at'.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/keys_endpoints.json",
      "rule": "type",
      "pointer_prefix": "#/paths//api/v1/keys/post/requestBody/content/application/json/schema/properties/scopes",
      "reason": "Not a migration consumers must make: the OLD schema described an API that never existed. Same string-to-array correction as the standalone create-request schema, kept in sync. Verified against the live route on TraigentBackend origin/develop, src/routes/api_key_routes.py:341-358 -- scopes/permissions are read as list[str]; the route never parses a space-delimited string.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/keys_endpoints.json",
      "rule": "minItems",
      "pointer_prefix": "#/paths//api/v1/keys/post/requestBody/content/application/json/schema/properties/scopes",
      "reason": "Not a migration consumers must make: the OLD schema described an API that never existed. Same minItems 1 as the standalone create-request schema, kept in sync. Verified against the live route on TraigentBackend origin/develop, src/routes/api_key_routes.py:341-358 -- scopes/permissions are read as list[str]; the route never parses a space-delimited string.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/keys_endpoints.json",
      "rule": "items",
      "pointer_prefix": "#/paths//api/v1/keys/post/requestBody/content/application/json/schema/properties/scopes/items",
      "reason": "Same maxLength 64 scope-token bound as the standalone create-request schema, kept in sync; see that file's items entry for the rationale.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/keys_endpoints.json",
      "rule": "property_added",
      "pointer_prefix": "#/paths//api/v1/keys/post/responses/201/content/application/json/schema/properties/data",
      "reason": "Not a migration consumers must make: the OLD schema described an API that never existed. The 201 body is a {success, message, data} envelope and 'data' is part of it; the old schema documented the inner object as if it were the whole response. Verified against the live route on TraigentBackend origin/develop, src/routes/api_key_routes.py:1263-1282 -- the 201 body is {success, message, data{key, key_id, key_prefix, key_name, key_type, created_at}}}}.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/keys_endpoints.json",
      "rule": "property_added",
      "pointer_prefix": "#/paths//api/v1/keys/post/responses/201/content/application/json/schema/properties/message",
      "reason": "Not a migration consumers must make: the OLD schema described an API that never existed. The 201 body is a {success, message, data} envelope and 'message' is part of it; the old schema documented the inner object as if it were the whole response. Verified against the live route on TraigentBackend origin/develop, src/routes/api_key_routes.py:1263-1282 -- the 201 body is {success, message, data{key, key_id, key_prefix, key_name, key_type, created_at}}}}.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/keys_endpoints.json",
      "rule": "property_added",
      "pointer_prefix": "#/paths//api/v1/keys/post/responses/201/content/application/json/schema/properties/success",
      "reason": "Not a migration consumers must make: the OLD schema described an API that never existed. The 201 body is a {success, message, data} envelope and 'success' is part of it; the old schema documented the inner object as if it were the whole response. Verified against the live route on TraigentBackend origin/develop, src/routes/api_key_routes.py:1263-1282 -- the 201 body is {success, message, data{key, key_id, key_prefix, key_name, key_type, created_at}}}}.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/keys_endpoints.json",
      "rule": "schema_removed",
      "pointer_prefix": "#/paths//api/v1/keys/post/responses/400/content/application/json/schema",
      "reason": "Not a removal: the 400 body moved from a duplicated inline schema to $ref #/components/responses/BadRequest in this same file, and that pointer was verified to resolve. The breaking-check does not follow $ref, so it reports the inline schema as gone; the contract this status code presents to a consumer is unchanged.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/keys_endpoints.json",
      "rule": "schema_removed",
      "pointer_prefix": "#/paths//api/v1/keys/post/responses/401/content/application/json/schema",
      "reason": "Not a removal: the 401 body moved from a duplicated inline schema to $ref #/components/responses/Unauthorized in this same file, and that pointer was verified to resolve. The breaking-check does not follow $ref, so it reports the inline schema as gone; the contract this status code presents to a consumer is unchanged.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/keys_endpoints.json",
      "rule": "schema_removed",
      "pointer_prefix": "#/paths//api/v1/keys/post/responses/403/content/application/json/schema",
      "reason": "Not a removal: the 403 body moved from a duplicated inline schema to $ref #/components/responses/Forbidden in this same file, and that pointer was verified to resolve. The breaking-check does not follow $ref, so it reports the inline schema as gone; the contract this status code presents to a consumer is unchanged.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/keys_endpoints.json",
      "rule": "schema_removed",
      "pointer_prefix": "#/paths//api/v1/keys/post/responses/409/content/application/json/schema",
      "reason": "Not a removal: the 409 body moved from a duplicated inline schema to $ref #/components/responses/Conflict in this same file, and that pointer was verified to resolve. The breaking-check does not follow $ref, so it reports the inline schema as gone; the contract this status code presents to a consumer is unchanged.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "auth/keys_endpoints.json",
      "rule": "schema_removed",
      "pointer_prefix": "#/paths//api/v1/keys/post/responses/500/content/application/json/schema",
      "reason": "Not a removal: the 500 body moved from a duplicated inline schema to $ref #/components/responses/InternalError in this same file, and that pointer was verified to resolve. The breaking-check does not follow $ref, so it reports the inline schema as gone; the contract this status code presents to a consumer is unchanged.",
      "version": "5.6.0",
      "pr": "#376"
    },
    {
      "file": "analytics/run_example_insights_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/example_rows/items/properties/example_id",
      "reason": "Adds the client-owned example_id (optional string, 1-255 chars) to example-insights rows so the coarse enums (priority, difficulty bucket, suspicious flags, recommended action) finally connect to the customer's OWN examples instead of only server-keyed HMAC exref_ hashes. Identity already crosses the client boundary today as /example-scoring/{run_id}/scores dict keys and as GuidancePlan seed_ref, so this widens no signal disclosure: the field is identity only, never a score, rank, or signal channel, and rows the backend cannot resolve simply omit it. Acknowledged as property_added on a closed object because an old strict response consumer that pinned the previous row shape may reject the newly-emitted member; that consumer must upgrade its schema rather than have the producer stay silent. safe_example_ref remains required and unchanged for joins.",
      "version": "5.7.0",
      "pr": "#424 (branch feat/example-insights-example-id, Spine-Trail st_4f05c04c9f41)"
    },
    {
      "file": "costs/cost_user_usage_item_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/cache_read_tokens",
      "reason": "Intentionally adds this optional cached-token dimension to the closed aggregate usage response for target release 5.8.0. Accurate cached-token pricing requires nullable unknown-vs-zero counts and the TTL split rather than a guessed write rate. Existing producers need not emit it, but a strict response consumer must adopt the 5.8 contract before producer emission. This acknowledgement is scoped to the exact property so unrelated future additions remain breaking.",
      "version": "5.8.0",
      "pr": "#379"
    },
    {
      "file": "costs/cost_user_usage_item_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/cache_creation_tokens",
      "reason": "Intentionally adds this optional cached-token dimension to the closed aggregate usage response for target release 5.8.0. Accurate cached-token pricing requires nullable unknown-vs-zero counts and the TTL split rather than a guessed write rate. Existing producers need not emit it, but a strict response consumer must adopt the 5.8 contract before producer emission. This acknowledgement is scoped to the exact property so unrelated future additions remain breaking.",
      "version": "5.8.0",
      "pr": "#379"
    },
    {
      "file": "costs/cost_user_usage_item_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/cache_creation_tokens_by_ttl",
      "reason": "Intentionally adds this optional cached-token dimension to the closed aggregate usage response for target release 5.8.0. Accurate cached-token pricing requires nullable unknown-vs-zero counts and the TTL split rather than a guessed write rate. Existing producers need not emit it, but a strict response consumer must adopt the 5.8 contract before producer emission. This acknowledgement is scoped to the exact property so unrelated future additions remain breaking.",
      "version": "5.8.0",
      "pr": "#379"
    },
    {
      "file": "costs/cost_user_usage_item_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/unreported_usage_fields",
      "reason": "Intentionally adds this optional cached-token dimension to the closed aggregate usage response for target release 5.8.0. Accurate cached-token pricing requires nullable unknown-vs-zero counts and the TTL split rather than a guessed write rate. Existing producers need not emit it, but a strict response consumer must adopt the 5.8 contract before producer emission. This acknowledgement is scoped to the exact property so unrelated future additions remain breaking.",
      "version": "5.8.0",
      "pr": "#379"
    },
    {
      "file": "observability/observation_ingest_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/cache_read_tokens",
      "reason": "Intentionally adds this optional cached-token dimension to the closed observation-ingest request at this exact bounded nesting depth for target release 5.8.0. Existing requests remain valid, but a newer SDK request would be rejected by the current backend ObservationPayload (extra=forbid) until TraigentBackend#2511 lands. Schema therefore lands first; Backend acceptance and parity must land before SDK producer emission. This acknowledgement is scoped to the exact property so unrelated future additions remain breaking.",
      "version": "5.8.0",
      "pr": "#379"
    },
    {
      "file": "observability/observation_ingest_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/cache_creation_tokens",
      "reason": "Intentionally adds this optional cached-token dimension to the closed observation-ingest request at this exact bounded nesting depth for target release 5.8.0. Existing requests remain valid, but a newer SDK request would be rejected by the current backend ObservationPayload (extra=forbid) until TraigentBackend#2511 lands. Schema therefore lands first; Backend acceptance and parity must land before SDK producer emission. This acknowledgement is scoped to the exact property so unrelated future additions remain breaking.",
      "version": "5.8.0",
      "pr": "#379"
    },
    {
      "file": "observability/observation_ingest_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/cache_creation_tokens_by_ttl",
      "reason": "Intentionally adds this optional cached-token dimension to the closed observation-ingest request at this exact bounded nesting depth for target release 5.8.0. Existing requests remain valid, but a newer SDK request would be rejected by the current backend ObservationPayload (extra=forbid) until TraigentBackend#2511 lands. Schema therefore lands first; Backend acceptance and parity must land before SDK producer emission. This acknowledgement is scoped to the exact property so unrelated future additions remain breaking.",
      "version": "5.8.0",
      "pr": "#379"
    },
    {
      "file": "observability/observation_ingest_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/unreported_usage_fields",
      "reason": "Intentionally adds this optional cached-token dimension to the closed observation-ingest request at this exact bounded nesting depth for target release 5.8.0. Existing requests remain valid, but a newer SDK request would be rejected by the current backend ObservationPayload (extra=forbid) until TraigentBackend#2511 lands. Schema therefore lands first; Backend acceptance and parity must land before SDK producer emission. This acknowledgement is scoped to the exact property so unrelated future additions remain breaking.",
      "version": "5.8.0",
      "pr": "#379"
    },
    {
      "file": "observability/observation_ingest_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/children/items/properties/cache_read_tokens",
      "reason": "Intentionally adds this optional cached-token dimension to the closed observation-ingest request at this exact bounded nesting depth for target release 5.8.0. Existing requests remain valid, but a newer SDK request would be rejected by the current backend ObservationPayload (extra=forbid) until TraigentBackend#2511 lands. Schema therefore lands first; Backend acceptance and parity must land before SDK producer emission. This acknowledgement is scoped to the exact property so unrelated future additions remain breaking.",
      "version": "5.8.0",
      "pr": "#379"
    },
    {
      "file": "observability/observation_ingest_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/children/items/properties/cache_creation_tokens",
      "reason": "Intentionally adds this optional cached-token dimension to the closed observation-ingest request at this exact bounded nesting depth for target release 5.8.0. Existing requests remain valid, but a newer SDK request would be rejected by the current backend ObservationPayload (extra=forbid) until TraigentBackend#2511 lands. Schema therefore lands first; Backend acceptance and parity must land before SDK producer emission. This acknowledgement is scoped to the exact property so unrelated future additions remain breaking.",
      "version": "5.8.0",
      "pr": "#379"
    },
    {
      "file": "observability/observation_ingest_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/children/items/properties/cache_creation_tokens_by_ttl",
      "reason": "Intentionally adds this optional cached-token dimension to the closed observation-ingest request at this exact bounded nesting depth for target release 5.8.0. Existing requests remain valid, but a newer SDK request would be rejected by the current backend ObservationPayload (extra=forbid) until TraigentBackend#2511 lands. Schema therefore lands first; Backend acceptance and parity must land before SDK producer emission. This acknowledgement is scoped to the exact property so unrelated future additions remain breaking.",
      "version": "5.8.0",
      "pr": "#379"
    },
    {
      "file": "observability/observation_ingest_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/children/items/properties/unreported_usage_fields",
      "reason": "Intentionally adds this optional cached-token dimension to the closed observation-ingest request at this exact bounded nesting depth for target release 5.8.0. Existing requests remain valid, but a newer SDK request would be rejected by the current backend ObservationPayload (extra=forbid) until TraigentBackend#2511 lands. Schema therefore lands first; Backend acceptance and parity must land before SDK producer emission. This acknowledgement is scoped to the exact property so unrelated future additions remain breaking.",
      "version": "5.8.0",
      "pr": "#379"
    },
    {
      "file": "observability/observation_ingest_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/children/items/properties/children/items/properties/cache_read_tokens",
      "reason": "Intentionally adds this optional cached-token dimension to the closed observation-ingest request at this exact bounded nesting depth for target release 5.8.0. Existing requests remain valid, but a newer SDK request would be rejected by the current backend ObservationPayload (extra=forbid) until TraigentBackend#2511 lands. Schema therefore lands first; Backend acceptance and parity must land before SDK producer emission. This acknowledgement is scoped to the exact property so unrelated future additions remain breaking.",
      "version": "5.8.0",
      "pr": "#379"
    },
    {
      "file": "observability/observation_ingest_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/children/items/properties/children/items/properties/cache_creation_tokens",
      "reason": "Intentionally adds this optional cached-token dimension to the closed observation-ingest request at this exact bounded nesting depth for target release 5.8.0. Existing requests remain valid, but a newer SDK request would be rejected by the current backend ObservationPayload (extra=forbid) until TraigentBackend#2511 lands. Schema therefore lands first; Backend acceptance and parity must land before SDK producer emission. This acknowledgement is scoped to the exact property so unrelated future additions remain breaking.",
      "version": "5.8.0",
      "pr": "#379"
    },
    {
      "file": "observability/observation_ingest_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/children/items/properties/children/items/properties/cache_creation_tokens_by_ttl",
      "reason": "Intentionally adds this optional cached-token dimension to the closed observation-ingest request at this exact bounded nesting depth for target release 5.8.0. Existing requests remain valid, but a newer SDK request would be rejected by the current backend ObservationPayload (extra=forbid) until TraigentBackend#2511 lands. Schema therefore lands first; Backend acceptance and parity must land before SDK producer emission. This acknowledgement is scoped to the exact property so unrelated future additions remain breaking.",
      "version": "5.8.0",
      "pr": "#379"
    },
    {
      "file": "observability/observation_ingest_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/children/items/properties/children/items/properties/unreported_usage_fields",
      "reason": "Intentionally adds this optional cached-token dimension to the closed observation-ingest request at this exact bounded nesting depth for target release 5.8.0. Existing requests remain valid, but a newer SDK request would be rejected by the current backend ObservationPayload (extra=forbid) until TraigentBackend#2511 lands. Schema therefore lands first; Backend acceptance and parity must land before SDK producer emission. This acknowledgement is scoped to the exact property so unrelated future additions remain breaking.",
      "version": "5.8.0",
      "pr": "#379"
    },
    {
      "file": "observability/observation_ingest_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/children/items/properties/children/items/properties/children/items/properties/cache_read_tokens",
      "reason": "Intentionally adds this optional cached-token dimension to the closed observation-ingest request at this exact bounded nesting depth for target release 5.8.0. Existing requests remain valid, but a newer SDK request would be rejected by the current backend ObservationPayload (extra=forbid) until TraigentBackend#2511 lands. Schema therefore lands first; Backend acceptance and parity must land before SDK producer emission. This acknowledgement is scoped to the exact property so unrelated future additions remain breaking.",
      "version": "5.8.0",
      "pr": "#379"
    },
    {
      "file": "observability/observation_ingest_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/children/items/properties/children/items/properties/children/items/properties/cache_creation_tokens",
      "reason": "Intentionally adds this optional cached-token dimension to the closed observation-ingest request at this exact bounded nesting depth for target release 5.8.0. Existing requests remain valid, but a newer SDK request would be rejected by the current backend ObservationPayload (extra=forbid) until TraigentBackend#2511 lands. Schema therefore lands first; Backend acceptance and parity must land before SDK producer emission. This acknowledgement is scoped to the exact property so unrelated future additions remain breaking.",
      "version": "5.8.0",
      "pr": "#379"
    },
    {
      "file": "observability/observation_ingest_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/children/items/properties/children/items/properties/children/items/properties/cache_creation_tokens_by_ttl",
      "reason": "Intentionally adds this optional cached-token dimension to the closed observation-ingest request at this exact bounded nesting depth for target release 5.8.0. Existing requests remain valid, but a newer SDK request would be rejected by the current backend ObservationPayload (extra=forbid) until TraigentBackend#2511 lands. Schema therefore lands first; Backend acceptance and parity must land before SDK producer emission. This acknowledgement is scoped to the exact property so unrelated future additions remain breaking.",
      "version": "5.8.0",
      "pr": "#379"
    },
    {
      "file": "observability/observation_ingest_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/children/items/properties/children/items/properties/children/items/properties/unreported_usage_fields",
      "reason": "Intentionally adds this optional cached-token dimension to the closed observation-ingest request at this exact bounded nesting depth for target release 5.8.0. Existing requests remain valid, but a newer SDK request would be rejected by the current backend ObservationPayload (extra=forbid) until TraigentBackend#2511 lands. Schema therefore lands first; Backend acceptance and parity must land before SDK producer emission. This acknowledgement is scoped to the exact property so unrelated future additions remain breaking.",
      "version": "5.8.0",
      "pr": "#379"
    },
    {
      "file": "observability/observation_ingest_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/children/items/properties/children/items/properties/children/items/properties/children/items/properties/cache_read_tokens",
      "reason": "Intentionally adds this optional cached-token dimension to the closed observation-ingest request at this exact bounded nesting depth for target release 5.8.0. Existing requests remain valid, but a newer SDK request would be rejected by the current backend ObservationPayload (extra=forbid) until TraigentBackend#2511 lands. Schema therefore lands first; Backend acceptance and parity must land before SDK producer emission. This acknowledgement is scoped to the exact property so unrelated future additions remain breaking.",
      "version": "5.8.0",
      "pr": "#379"
    },
    {
      "file": "observability/observation_ingest_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/children/items/properties/children/items/properties/children/items/properties/children/items/properties/cache_creation_tokens",
      "reason": "Intentionally adds this optional cached-token dimension to the closed observation-ingest request at this exact bounded nesting depth for target release 5.8.0. Existing requests remain valid, but a newer SDK request would be rejected by the current backend ObservationPayload (extra=forbid) until TraigentBackend#2511 lands. Schema therefore lands first; Backend acceptance and parity must land before SDK producer emission. This acknowledgement is scoped to the exact property so unrelated future additions remain breaking.",
      "version": "5.8.0",
      "pr": "#379"
    },
    {
      "file": "observability/observation_ingest_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/children/items/properties/children/items/properties/children/items/properties/children/items/properties/cache_creation_tokens_by_ttl",
      "reason": "Intentionally adds this optional cached-token dimension to the closed observation-ingest request at this exact bounded nesting depth for target release 5.8.0. Existing requests remain valid, but a newer SDK request would be rejected by the current backend ObservationPayload (extra=forbid) until TraigentBackend#2511 lands. Schema therefore lands first; Backend acceptance and parity must land before SDK producer emission. This acknowledgement is scoped to the exact property so unrelated future additions remain breaking.",
      "version": "5.8.0",
      "pr": "#379"
    },
    {
      "file": "observability/observation_ingest_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/children/items/properties/children/items/properties/children/items/properties/children/items/properties/unreported_usage_fields",
      "reason": "Intentionally adds this optional cached-token dimension to the closed observation-ingest request at this exact bounded nesting depth for target release 5.8.0. Existing requests remain valid, but a newer SDK request would be rejected by the current backend ObservationPayload (extra=forbid) until TraigentBackend#2511 lands. Schema therefore lands first; Backend acceptance and parity must land before SDK producer emission. This acknowledgement is scoped to the exact property so unrelated future additions remain breaking.",
      "version": "5.8.0",
      "pr": "#379"
    },
    {
      "file": "observability/observation_ingest_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/children/items/properties/children/items/properties/children/items/properties/children/items/properties/children/items/properties/cache_read_tokens",
      "reason": "Intentionally adds this optional cached-token dimension to the closed observation-ingest request at this exact bounded nesting depth for target release 5.8.0. Existing requests remain valid, but a newer SDK request would be rejected by the current backend ObservationPayload (extra=forbid) until TraigentBackend#2511 lands. Schema therefore lands first; Backend acceptance and parity must land before SDK producer emission. This acknowledgement is scoped to the exact property so unrelated future additions remain breaking.",
      "version": "5.8.0",
      "pr": "#379"
    },
    {
      "file": "observability/observation_ingest_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/children/items/properties/children/items/properties/children/items/properties/children/items/properties/children/items/properties/cache_creation_tokens",
      "reason": "Intentionally adds this optional cached-token dimension to the closed observation-ingest request at this exact bounded nesting depth for target release 5.8.0. Existing requests remain valid, but a newer SDK request would be rejected by the current backend ObservationPayload (extra=forbid) until TraigentBackend#2511 lands. Schema therefore lands first; Backend acceptance and parity must land before SDK producer emission. This acknowledgement is scoped to the exact property so unrelated future additions remain breaking.",
      "version": "5.8.0",
      "pr": "#379"
    },
    {
      "file": "observability/observation_ingest_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/children/items/properties/children/items/properties/children/items/properties/children/items/properties/children/items/properties/cache_creation_tokens_by_ttl",
      "reason": "Intentionally adds this optional cached-token dimension to the closed observation-ingest request at this exact bounded nesting depth for target release 5.8.0. Existing requests remain valid, but a newer SDK request would be rejected by the current backend ObservationPayload (extra=forbid) until TraigentBackend#2511 lands. Schema therefore lands first; Backend acceptance and parity must land before SDK producer emission. This acknowledgement is scoped to the exact property so unrelated future additions remain breaking.",
      "version": "5.8.0",
      "pr": "#379"
    },
    {
      "file": "observability/observation_ingest_schema.json",
      "rule": "property_added",
      "pointer_prefix": "#/properties/children/items/properties/children/items/properties/children/items/properties/children/items/properties/children/items/properties/unreported_usage_fields",
      "reason": "Intentionally adds this optional cached-token dimension to the closed observation-ingest request at this exact bounded nesting depth for target release 5.8.0. Existing requests remain valid, but a newer SDK request would be rejected by the current backend ObservationPayload (extra=forbid) until TraigentBackend#2511 lands. Schema therefore lands first; Backend acceptance and parity must land before SDK producer emission. This acknowledgement is scoped to the exact property so unrelated future additions remain breaking.",
      "version": "5.8.0",
      "pr": "#379"
    }
  ]
}
