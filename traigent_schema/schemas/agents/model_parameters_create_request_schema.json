{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schemas.traigent.ai/agents/model_parameters_create_request_schema.json",
  "title": "Model Parameters Schema Create Request",
  "description": "Create request for the Model Parameters Schema resource. Omits server-generated fields (id) from `required` — the backend generates ids/timestamps server-side and ignores a client-supplied value. additionalProperties:true so a client MAY still send a server field without rejection.",
  "type": "object",
  "required": [
    "model_id"
  ],
  "additionalProperties": true,
  "definitions": {
    "StopSequence": {
      "type": "string",
      "description": "A sequence of text that signals the model to stop generating further tokens"
    }
  },
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique identifier for this parameter configuration",
      "x-identifier": true
    },
    "model_id": {
      "type": "string",
      "description": "Reference to the model these parameters are configured for"
    },
    "temperature": {
      "type": "number",
      "minimum": 0,
      "maximum": 2,
      "default": 0.2,
      "description": "Controls randomness in the output generation:\n- 0.0: Deterministic, focused responses\n- 1.0: Balanced creativity\n- 2.0: Maximum creativity and randomness"
    },
    "max_tokens": {
      "type": "integer",
      "minimum": 1,
      "default": 4000,
      "description": "Maximum number of tokens to generate in the response. Helps control response length and processing time"
    },
    "top_p": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "default": 0.2,
      "description": "Controls diversity via nucleus sampling:\n- 0.1: Very focused on highest probability tokens\n- 0.5: Balanced selection\n- 1.0: Consider all tokens based on their probabilities"
    },
    "frequency_penalty": {
      "type": "number",
      "minimum": -2,
      "maximum": 2,
      "default": 0.2,
      "description": "Adjusts likelihood of token selection based on their frequency in the text:\n- Negative: Favor frequent tokens\n- 0: No adjustment\n- Positive: Penalize frequent tokens"
    },
    "presence_penalty": {
      "type": "number",
      "minimum": -2,
      "maximum": 2,
      "default": 0.2,
      "description": "Adjusts likelihood of token selection based on their presence in the text:\n- Negative: Favor used tokens\n- 0: No adjustment\n- Positive: Encourage new tokens"
    },
    "top_k": {
      "type": "integer",
      "minimum": 1,
      "default": 50,
      "description": "Limits token selection to the k highest probability tokens. Lower values increase focus, higher values increase diversity"
    },
    "repetition_penalty": {
      "type": "number",
      "minimum": 0,
      "default": 0.2,
      "description": "Factor applied to penalize repetition in generated text. Higher values make repetition less likely"
    },
    "stop_sequences": {
      "type": [
        "array",
        "null"
      ],
      "items": {
        "$ref": "#/definitions/StopSequence"
      },
      "default": null,
      "description": "List of sequences that will cause the model to stop generating further tokens",
      "examples": [
        [
          "###",
          "END",
          "```"
        ]
      ]
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp of when these parameters were created"
    },
    "updated_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp of the last modification to these parameters"
    },
    "metadata": {
      "type": "object",
      "description": "Additional metadata about the parameter configuration",
      "properties": {
        "tags": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Tags for categorizing and filtering parameter configurations"
        },
        "description": {
          "type": "string",
          "description": "Human-readable description of this parameter configuration's purpose"
        },
        "version": {
          "type": "string",
          "description": "Version of the parameter configuration",
          "pattern": "^\\d+\\.\\d+\\.\\d+$"
        }
      },
      "additionalProperties": true
    }
  }
}
