{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schemas.traigent.ai/auth/api_key_rotate_request_schema.json",
  "title": "API Key Rotate Request",
  "description": "Request body for POST /api/v1/keys/{key_id}/rotate.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "strategy": {
      "type": "string",
      "enum": ["immediate", "gradual", "scheduled", "emergency"],
      "default": "immediate",
      "description": "Rotation strategy."
    },
    "reason": {
      "type": "string",
      "maxLength": 255,
      "description": "Optional client-supplied reason."
    }
  },
  "x-backend-obligations": [
    "Rotating a key whose persisted key_type is director must produce a key whose persisted state satisfies the same director invariants (scopes exactly [\"director_evidence:read\"], empty broad permissions, at least one per-experiment read grant each expiring no later than 30 days, no wildcard or admin grant); otherwise 400 and nothing persists."
  ]
}
