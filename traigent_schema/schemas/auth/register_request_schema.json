{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schemas.traigent.ai/auth/register_request_schema.json",
  "title": "Register Request",
  "description": "POST /api/v1/auth/register body. additionalProperties:false \u2014 the server REJECTS unknown keys with a 400 rather than ignoring them, so a client that sends a retired or misspelled field is told, not silently registered without it.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "email": {
      "type": "string",
      "minLength": 1,
      "format": "email",
      "maxLength": 320,
      "description": "New account email."
    },
    "password": {
      "type": "string",
      "minLength": 1,
      "maxLength": 1024,
      "description": "New account password."
    },
    "name": {
      "type": "string",
      "maxLength": 255,
      "description": "Optional display name."
    },
    "organization": {
      "type": "string",
      "maxLength": 255,
      "description": "Optional organization name."
    },
    "role": {
      "type": "string",
      "maxLength": 64,
      "x-inert": true,
      "description": "Advisory-only. Backend always assigns the server-side default role (AUTH-002). Clients should omit this field; the contract may add an enum constraint in a future minor version."
    },
    "access_code": {
      "type": "string",
      "maxLength": 128,
      "description": "Optional. The administrator-issued cohort code. Distinct from registration_code: reusable within its cohort and not bound to one address. Mutually exclusive with the other two credential fields."
    },
    "tier": {
      "type": "string",
      "maxLength": 64,
      "description": "Optional requested registration tier."
    },
    "invite_token": {
      "type": "string",
      "maxLength": 512,
      "description": "Optional. Single-use invitation token. Mutually exclusive with the other two credential fields."
    },
    "registration_code": {
      "type": "string",
      "maxLength": 64,
      "description": "Optional. The single-use, address-bound code delivered by e-mail to a self-serve registrant. Presented to the reader as their \"access code\"; registration_code is the wire name and the two deliberately differ, because access_code is a different credential resolved against a different store. Mutually exclusive with the other two credential fields."
    }
  },
  "required": [
    "email",
    "password"
  ]
}
