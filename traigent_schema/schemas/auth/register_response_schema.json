{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schemas.traigent.ai/auth/register_response_schema.json",
  "title": "Register Response DTO",
  "description": "Deprecated flat HTTP 200 compatibility DTO retained for the 5.x validation window. Current TraigentBackend producers return HTTP 201 with the wrapped register_created_response_schema.json contract. Removing or tightening this accepted legacy shape is deferred to the next major version.",
  "type": "object",
  "additionalProperties": false,
  "required": ["success", "message", "user", "requires_email_verification", "email_sent"],
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string",
      "minLength": 1
    },
    "user": {
      "$ref": "./auth_user_identity_schema.json",
      "description": "Registered user identity. email_verified will be false until the user completes the email verification flow."
    },
    "requires_email_verification": {
      "type": "boolean",
      "description": "Always true for self-service registration — the account is inactive until the verification link is clicked."
    },
    "email_sent": {
      "type": "boolean",
      "description": "Whether the verification email was successfully dispatched. False only on transient delivery failure; the user can resend via POST /api/v1/auth/resend-verification."
    }
  }
}
