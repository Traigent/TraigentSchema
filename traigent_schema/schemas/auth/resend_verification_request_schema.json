{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schemas.traigent.ai/auth/resend_verification_request_schema.json",
  "title": "Resend Verification Request",
  "description": "POST /api/v1/auth/resend-verification body. additionalProperties:true (backend reads email and ignores unknown keys).",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "email": {
      "type": "string",
      "minLength": 1,
      "format": "email",
      "maxLength": 320,
      "description": "Email to resend the verification link to."
    }
  },
  "required": [
    "email"
  ]
}
