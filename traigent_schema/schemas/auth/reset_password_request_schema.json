{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schemas.traigent.ai/auth/reset_password_request_schema.json",
  "title": "Reset Password Request",
  "description": "POST /api/v1/auth/reset-password body. additionalProperties:true — the backend reads the documented keys and ignores unknown ones (never 422s on extras).",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "reset_token": {
      "type": "string",
      "minLength": 1,
      "maxLength": 2048,
      "description": "Password-reset token from the emailed link."
    },
    "new_password": {
      "type": "string",
      "minLength": 1,
      "maxLength": 1024,
      "description": "New password to set."
    }
  },
  "required": [
    "reset_token",
    "new_password"
  ]
}
