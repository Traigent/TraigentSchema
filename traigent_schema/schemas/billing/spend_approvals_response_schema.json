{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schemas.traigent.ai/billing/spend_approvals_response_schema.json",
  "title": "Spend Approvals Response",
  "description": "GET /api/v1/billing/spend-approvals — pending/historical spend approvals. Uses the billing success envelope {success, message, data} (success_response).",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": "array",
      "description": "Spend approval records.",
      "items": {
        "type": "object",
        "additionalProperties": true
      }
    }
  },
  "required": [
    "success",
    "data"
  ]
}
