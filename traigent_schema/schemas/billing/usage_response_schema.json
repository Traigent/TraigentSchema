{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schemas.traigent.ai/billing/usage_response_schema.json",
  "title": "Usage Response",
  "description": "GET /api/v1/billing/usage — per-metric usage. Backend returns usage_tracker.get_usage() fields (metric/used/limit/percentage/...); data is left open pending a confirmed @backend field map. Billing success envelope {success, message, data} (success_response).",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": [
        "object",
        "null"
      ],
      "additionalProperties": true
    }
  },
  "required": [
    "success",
    "message",
    "data"
  ]
}
