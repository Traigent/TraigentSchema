{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schemas.traigent.ai/billing/wallet_response_schema.json",
  "title": "Wallet Response",
  "description": "GET /api/v1/billing/wallet — current wallet state. Backend serializes monetary amounts as strings; data is left open pending a confirmed @backend field map. Billing success envelope {success, message, data} (success_response).",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "success": {
      "type": "boolean"
    },
    "message": {
      "type": "string"
    },
    "data": {
      "type": [
        "object",
        "null"
      ],
      "additionalProperties": true
    }
  },
  "required": [
    "success",
    "message",
    "data"
  ]
}
