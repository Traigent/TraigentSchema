{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schemas.traigent.ai/certification/agent_certificate_v0_schema.json",
  "title": "Agent Certificate",
  "description": "The Agent Certificate v0 envelope. The title and build-session scope line remain mandatory and the certificate is scoped to the named build session. B1 may be printed only as the narrow issuer_verified seal-statement assertion when the fixed ledger continuity/root and signing-envelope checks pass; G1 is the narrow client_declared unopened commitment and remains fixed at tier 1 with client co-attestation. REG1, C1, D2, F1, and G3 remain abstention-only absent separately registered verifier evidence. Every audit report has exactly seven ordered rows and one closed evidence_basis per row. The certificate contains no tenant_id or project_id; only the opaque caller-pinned project_ref is public. All fields remain closed and content-free; no deployment, completeness, correctness, quality, correspondence, possession, or omission-resistance claim is implied.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "certificate_title",
    "build_session_scope_line",
    "certificate_scope_line",
    "subject",
    "semantics",
    "disclosure_profile",
    "ledger_seal_projection",
    "claims",
    "non_claims",
    "signatures",
    "audit_report"
  ],
  "properties": {
    "schema_version": {
      "description": "Exact-version wire discriminator; verification dispatches on it plus the signed semantics-manifest digest. An unknown value fails closed.",
      "const": "traigent.agent_certificate.v0"
    },
    "certificate_title": {
      "description": "The document title, pinned. 'retain Agent Certificate' (C6 Ruling 3(v), reconciled).",
      "const": "Agent Certificate"
    },
    "build_session_scope_line": {
      "description": "The prominent build-session scope line, printed immediately below the title (mandatory page-one scope box, conceded to the proposer).",
      "const": "Build-session scope: this certificate concerns a build session, not the deployed agent. It makes no deployment-identity, deployment-binding, drift-detection, or deployed-runtime claim."
    },
    "certificate_scope_line": {
      "description": "The exact v0 honesty scope line, printed adjacent to the title and build-session scope line. Its const value is part of the closed certificate envelope.",
      "const": "Scope: Certified Agent v0 verifies signatures, build-session binding, and an unopened client declaration. It does not certify the contents, quality, correctness, correspondence, possession, or completeness of the agent, evaluation dataset, evaluator, or build-process evidence."
    },
    "subject": {
      "$ref": "certification_common_v0_schema.json#/definitions/BuildSessionSubjectV0"
    },
    "semantics": {
      "$ref": "#/definitions/SemanticsBindingV0"
    },
    "disclosure_profile": {
      "$ref": "#/definitions/DisclosureProfileV0"
    },
    "ledger_seal_projection": {
      "description": "The sealed expected-stream projection this certificate's claim B1 (if printed) refers to. Legacy/unsealed sessions are refused issuance, never silently downgraded (C6 Ruling 1, conceded) — the refusal is compiler-enforced (rejection code legacy_or_unsealed_session_refused).",
      "$ref": "certificate_ledger_seals_v0_schema.json#/definitions/SealStatementV0"
    },
    "claims": {
      "description": "The printed claims — possibly empty, since every v0 claim is conditional and 'Current evidence supports no presently issuable signed v0 certificate.' An early certificate has fewer and lower-tier claims, never absent caveats. This field MUST be copied verbatim into the signed unsigned-manifest's `claims` coverage section (certificate_unsigned_manifest_v0_schema.json#/definitions/UnsignedManifestV0); both reference certificate_claims_v0_schema.json#/definitions/ClaimsArrayV0 (SCHEMA-1, 2026-08-24) so the two copies are structurally the identical type, not merely described as such.",
      "$ref": "certificate_claims_v0_schema.json#/definitions/ClaimsArrayV0"
    },
    "non_claims": {
      "description": "The FOURTEEN mandatory printed NON-claims, tuple-validated in canonical print order: A3, A4, B2, E2, E3, F3, G2, H1, then the six standing non-claims. All fourteen are required; additionalItems:false plus the min/max bounds make omission, addition, and reordering schema-invalid. Non-claims are printed, never implied by absence. This field MUST be copied verbatim into the signed unsigned-manifest's `non_claims` coverage section (certificate_unsigned_manifest_v0_schema.json#/definitions/UnsignedManifestV0); both reference certificate_claims_v0_schema.json#/definitions/NonClaimsFixedTupleV0 (SCHEMA-1, 2026-08-24) so the two copies are structurally the identical type, not merely described as such.",
      "$ref": "certificate_claims_v0_schema.json#/definitions/NonClaimsFixedTupleV0"
    },
    "signatures": {
      "$ref": "certificate_signatures_v0_schema.json#/definitions/CertificateSignaturesV0"
    },
    "audit_report": {
      "description": "The complete typed pre-issuance audit report (SCHEMA-1 FINAL fix, 2026-08-24), whose role-separated digest is always the FIRST signed unsigned-manifest `evidence_digests` entry, including when `claims` is empty (all seven rows abstain); each printed claim additionally carries the same `audit_report_digest` evidence value. An independent verifier obtains it directly from this certificate, never from an out-of-band issuer database. The exact digest is `sha256:` plus lowercase SHA-256 over UTF8(`traigent.agent_certificate.audit_report.v1`) || 0x00 || jcs_v1(this field); that equality, and the report's own cross-field bindings to subject/seal/semantics/claims, are verifier obligations Draft-07 cannot check directly, exercised by tests/test_agent_certificate_v0_schemas.py's contract-verifier helper.",
      "$ref": "certificate_audit_report_v0_schema.json#/definitions/AuditReportV0"
    }
  },
  "allOf": [
    {
      "description": "Tier-1 claims require a real client co-attestation: 'tier 1 additionally needs a real client attestation' (C6 tier reality) and 'missing co-attestation should refuse issuance' (conceded). A certificate carrying any tier-1 claim without a co_attestation block is schema-invalid.",
      "if": {
        "properties": {
          "claims": {
            "contains": {
              "properties": {
                "tier": {
                  "const": 1
                }
              },
              "required": ["tier"]
            }
          }
        },
        "required": ["claims"]
      },
      "then": {
        "properties": {
          "signatures": {
            "required": ["co_attestation"]
          }
        }
      }
    },
    {
      "description": "A certificate with no tier-1 or G1 claim must not carry client co-attestation material. Co-attestation is reserved for the narrow client-declared G1/tier-1 path; B1-only certificates are issuer-only.",
      "if": {
        "properties": {
          "claims": {
            "not": {
              "contains": {
                "anyOf": [
                  {
                    "properties": {"tier": {"const": 1}},
                    "required": ["tier"]
                  },
                  {
                    "properties": {"claim_id": {"const": "G1"}},
                    "required": ["claim_id"]
                  }
                ]
              }
            }
          }
        },
        "required": ["claims"]
      },
      "then": {
        "properties": {
          "signatures": {
            "not": {"required": ["co_attestation"]}
          }
        }
      }
    }
  ],
  "definitions": {
    "SemanticsBindingV0": {
      "description": "The signed semantics reference for exact-version dispatch (C1 Ruling 6): the semantics manifest 'binds canonical schema definitions, claim templates, prohibited register, verifier catalog, tier semantics, signature scope, and canonicalization rules.' Each digest names one pinned, signed, ARCHIVED artifact — 'A digest without an archived, authentic bundle and resolver does not preserve verification', so archives and trust-manifest authority are B5/verifier obligations recorded here as scope.",
      "type": "object",
      "additionalProperties": false,
      "required": ["semantics_manifest_digest", "claim_template_catalog_digest", "prohibited_register_digest", "verifier_catalog_digest", "non_claim_reason_catalog_digest"],
      "properties": {
        "semantics_manifest_digest": {
          "$ref": "certification_common_v0_schema.json#/definitions/Sha256Digest"
        },
        "claim_template_catalog_digest": {
          "$ref": "certification_common_v0_schema.json#/definitions/Sha256Digest"
        },
        "prohibited_register_digest": {
          "description": "Digest of the pinned prohibited-claims register (P-1…P-40 plus the merged Wave-C additions once deduplicated and re-pinned; a register change that would reject a previously-emitted claim is a MAJOR version event).",
          "$ref": "certification_common_v0_schema.json#/definitions/Sha256Digest"
        },
        "verifier_catalog_digest": {
          "$ref": "certification_common_v0_schema.json#/definitions/Sha256Digest"
        },
        "non_claim_reason_catalog_digest": {
          "$ref": "certification_common_v0_schema.json#/definitions/Sha256Digest"
        }
      }
    },
    "DisclosureProfileV0": {
      "description": "The disclosure profile identification required by the overturned all-or-nothing rule: identify the profile and state that selective disclosure is not proven (G2 is simultaneously a mandatory printed non-claim).",
      "type": "object",
      "additionalProperties": false,
      "required": ["profile_id", "selective_disclosure_line"],
      "properties": {
        "profile_id": {
          "type": "string",
          "enum": ["public", "customer_internal", "auditor_only"]
        },
        "selective_disclosure_line": {
          "const": "Data omitted from this disclosure profile is simply absent; selective disclosure is not proven."
        }
      }
    }
  }
}
