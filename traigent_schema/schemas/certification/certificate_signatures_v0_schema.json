{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schemas.traigent.ai/certification/certificate_signatures_v0_schema.json",
  "title": "Agent Certificate v0 Signature and Co-Attestation Wire Shapes",
  "description": "Issuer-signature scope and client co-attestation block for Agent Certificate v0. The issuer projection excludes the outer client co_attestation and signs the canonical unsigned manifest first, not the manifest digest: issuer signed bytes are UTF8(`traigent.agent_certificate.issuer_signature.v0`) || uint64be(len(manifest_jcs_bytes)) || manifest_jcs_bytes. The client projection includes issuer_signature and excludes only the outer `co_attestation` member: client signed bytes are UTF8(`traigent.agent_certificate.client_co_attestation.v0`) || uint64be(len(certificate_projection_jcs_bytes)) || certificate_projection_jcs_bytes. Neither signature covers the other signature's raw bytes. The manifest_digest and signed_manifest_digest remain transmitted role-domain digest references and are not signature preimages. This family does NOT reuse ArtifactSignatureEnvelopeV2 — reuse was struck because adding nested signatures and new fields changes the format and verifier. The v0 issuer path is a dedicated per-environment certificate-signing key through protected GitHub-OIDC CI; the trust ring must be a signed, versioned artifact whose root fingerprint is pinned in the independent verifier. SCHEMA-1 P0 FIX: UnsignedManifestRefV0 carries the complete typed manifest document alongside its digest and fixed coverage list. Root instance is one CertificateSignaturesV0.",
  "definitions": {
    "UnsignedManifestCoverageV0": {
      "description": "The FIXED coverage list of the canonical unsigned manifest — the twelve sections C6 Ruling 3(iv) enumerates plus the signed disclosure-profile binding, pinned as a const so a manifest that covers less is unrepresentable. Freshness data is covered material; per-certificate expiry semantics are pinned by the signed semantics manifest. SCHEMA-1 (2026-08-25): certificate_unsigned_manifest_v0_schema.json#/definitions/UnsignedManifestV0 is the typed document these twelve labels name — each label is a required property of that object with concrete canonical preimage data, closing gap C1 ('coverage is a label, not a binding') and the mutable disclosure-profile gap. This file does not $ref that document (avoiding a dependency cycle back through this file's own consumers); the correspondence is enforced by keeping this const list and UnsignedManifestV0's required-property list identical, checked in tests/test_agent_certificate_v0_schemas.py.",
      "const": [
        "subject",
        "seal",
        "claims",
        "tiers",
        "evidence_digests",
        "non_claims",
        "disclosure_profile",
        "privacy_mode",
        "sdk_identity",
        "compiler_register_versions",
        "key_ring_identifiers",
        "freshness"
      ]
    },
    "UnsignedManifestRefV0": {
      "description": "The canonical unsigned manifest AS TRANSMITTED (SCHEMA-1 P0 fix, 2026-08-25): `document` is the complete typed certificate_unsigned_manifest_v0_schema.json#/definitions/UnsignedManifestV0 object, including the signed disclosure-profile projection. `manifest_digest` is computed over that complete content-free document; an independent verifier now needs no issuer database to obtain the manifest it is checking a signature over.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "manifest_digest",
        "coverage",
        "document"
      ],
      "properties": {
        "manifest_digest": {
          "$ref": "certification_common_v0_schema.json#/definitions/Sha256Digest"
        },
        "coverage": {
          "$ref": "#/definitions/UnsignedManifestCoverageV0"
        },
        "document": {
          "$ref": "certificate_unsigned_manifest_v0_schema.json#/definitions/UnsignedManifestV0"
        }
      }
    },
    "CoAttestationV0": {
      "description": "The client co-attestation block. The client signs the complete canonical issuer-signed certificate projection, never a digest and never a spine, with only this outer co_attestation member excluded. The projection includes the audit report's ordered rows, evidence_basis values, abstentions, claims, and issuer signature. `signed_manifest_digest` is a transmitted role-domain digest of that projection; the verifier recomputes it and never trusts the transmitted value. The project-scoped client key reference crosses as ClientKeyRefV0, not raw public-key bytes. NONCE BINDING: `nonce` MUST equal the manifest's freshness.nonce; a verifier compares both values after decoding the transmitted document.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "algorithm",
        "client_key_ref",
        "signed_manifest_digest",
        "nonce",
        "signature"
      ],
      "properties": {
        "algorithm": {
          "$ref": "certification_common_v0_schema.json#/definitions/SignatureAlgorithmV0"
        },
        "client_key_ref": {
          "description": "Opaque reference to the enrolled client signing key. A stable pseudonymous identifier with a linkability cost (C1, tension T3); never raw public-key bytes in v0.",
          "$ref": "certification_common_v0_schema.json#/definitions/ClientKeyRefV0"
        },
        "signed_manifest_digest": {
          "description": "Role-domain digest of the canonical issuer-signed certificate projection with only the outer co_attestation member removed. The verifier recomputes this value from the received certificate and rejects omissions or tampering.",
          "$ref": "certification_common_v0_schema.json#/definitions/Sha256Digest"
        },
        "nonce": {
          "description": "Public anti-replay nonce. Register entry 8 wording travels verbatim: a public nonce 'does not prevent dictionary guessing.'",
          "$ref": "certification_common_v0_schema.json#/definitions/PublicNonce"
        },
        "signature": {
          "$ref": "certification_common_v0_schema.json#/definitions/SignatureBytes"
        }
      }
    },
    "IssuerSignatureV0": {
      "description": "The issuer signature. The issuer signs the exact canonical unsigned-manifest bytes first: UTF8(`traigent.agent_certificate.issuer_signature.v0`) || uint64be(len(manifest_jcs_bytes)) || manifest_jcs_bytes. The issuer preimage never contains co_attestation or any client signature. The signing workflow must verify the prepared manifest and ledger before signing; this shape does not prove that workflow. ECDSA P-256 signatures use fixed IEEE-P1363 r||s and the verifier rejects high-S. KEY-REF BINDING: issuer_key_ref and trust_ring_ref MUST equal the manifest's key-ring identifiers.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "algorithm",
        "issuer_key_ref",
        "trust_ring_ref",
        "signed_payload",
        "signature"
      ],
      "properties": {
        "algorithm": {
          "$ref": "certification_common_v0_schema.json#/definitions/SignatureAlgorithmV0"
        },
        "issuer_key_ref": {
          "description": "Opaque reference to the dedicated per-environment certificate-signing key. Never the development Planner key; the general backend pod holds no certificate-signing authority (C6 Ruling 4, binding).",
          "$ref": "certification_common_v0_schema.json#/definitions/OpaqueRef"
        },
        "trust_ring_ref": {
          "description": "Opaque reference to the signed, versioned trust-ring artifact (with version) under which the issuer key is published. Offline verification additionally needs 'a compiled minimum checkpoint or another pinned release policy' against rollback (C6 Ruling 4) — a verifier obligation, not representable here.",
          "$ref": "certification_common_v0_schema.json#/definitions/OpaqueRef"
        },
        "signed_payload": {
          "description": "The issuer always signs the unsigned manifest first. A client co-attestation is a subsequent signature over the complete issuer-signed certificate projection and is not included in the issuer preimage.",
          "const": ["unsigned_manifest"]
        },
        "signature": {
          "$ref": "certification_common_v0_schema.json#/definitions/SignatureBytes"
        }
      }
    },
    "CertificateSignaturesV0": {
      "description": "The complete signature block: canonical unsigned-manifest reference, optional client co-attestation, and the issuer signature. The issuer signature always covers only the unsigned manifest (`signed_payload = [unsigned_manifest]`). When present, co_attestation is a subsequent client signature over the complete issuer-signed certificate projection with only the outer co_attestation member removed. 'Missing co-attestation should refuse issuance' for any tier-1 claim — that refusal is enforced by the certificate envelope and the server-side finalize state transition, not silently downgraded. B1-only certificates may issue with issuer material alone.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "unsigned_manifest",
        "issuer_signature"
      ],
      "properties": {
        "unsigned_manifest": {
          "$ref": "#/definitions/UnsignedManifestRefV0"
        },
        "co_attestation": {
          "$ref": "#/definitions/CoAttestationV0"
        },
        "issuer_signature": {
          "$ref": "#/definitions/IssuerSignatureV0"
        }
      }
    }
  },
  "allOf": [
    {
      "$ref": "#/definitions/CertificateSignaturesV0"
    }
  ]
}
