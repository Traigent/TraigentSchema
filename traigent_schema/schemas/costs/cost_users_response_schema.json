{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schemas.traigent.ai/costs/cost_users_response_schema.json",
  "title": "Cost Users Response",
  "description": "Success envelope for GET /api/v1/costs/users.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "success": { "type": "boolean" },
    "message": { "type": "string", "minLength": 1 },
    "data": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "items": {
          "type": "array",
          "items": { "$ref": "./cost_user_usage_item_schema.json" }
        },
        "page": { "type": "integer", "minimum": 1 },
        "per_page": { "type": "integer", "minimum": 1, "maximum": 100 },
        "total": { "type": "integer", "minimum": 0 },
        "total_pages": { "type": "integer", "minimum": 0, "description": "Optional canonical pagination field (see pagination_schema.json). Present when the server emits the full pagination set; the base response is the flat {page, per_page, total} dialect." },
        "has_next": { "type": "boolean", "description": "Optional canonical pagination field (see pagination_schema.json): whether a next page exists." },
        "has_prev": { "type": "boolean", "description": "Optional canonical pagination field (see pagination_schema.json): whether a previous page exists." },
        "sort": {
          "type": "string",
          "enum": [
            "total_cost",
            "total_tokens",
            "input_tokens",
            "output_tokens",
            "request_count",
            "last_activity"
          ]
        },
        "order": { "type": "string", "enum": ["asc", "desc"] },
        "period": { "type": "string", "minLength": 1 },
        "start_date": { "type": ["string", "null"], "format": "date-time" },
        "end_date": { "type": ["string", "null"], "format": "date-time" },
        "generated_at": { "type": "string", "format": "date-time" }
      },
      "required": [
        "items",
        "page",
        "per_page",
        "total",
        "sort",
        "order",
        "period",
        "start_date",
        "end_date",
        "generated_at"
      ]
    }
  },
  "required": ["success", "message", "data"]
}
