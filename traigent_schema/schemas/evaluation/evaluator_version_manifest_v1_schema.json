{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schemas.traigent.ai/evaluation/evaluator_version_manifest_v1_schema.json",
  "title": "Evaluator Version Manifest V1",
  "description": "What an evaluator version IS under traigent.content_identity.v1: everything that changes a score. evaluator_version_digest = 'sha256:' + lowercase hex SHA-256(UTF8('traigent.content_identity.evaluator_version.v1') || 0x00 || jcs_v1(EvaluatorVersionManifestV1)). Unkeyed: the manifest carries digests, model ids and version strings only. This spec does not change fp2 or efp2; it only consumes the efp2 digest as code_digest. Reference: traigent_schema.example_identity.compute_evaluator_version_digest. Normative text: docs/identity/content-identity-v1.md section 9. Every string pattern in this file ends with the portable end anchor (?![\\s\\S]) instead of $: in Python's re (which Python jsonschema uses) $ also matches before a trailing newline, so '^x$' would accept 'x\\n' -- a second spelling of one identifier. (?![\\s\\S]) means end-of-string in both ECMA-262 and Python.",
  "version": "1.0.0",
  "definitions": {
    "Sha256DigestV1": {
      "type": "string",
      "pattern": "^sha256:[0-9a-f]{64}(?![\\s\\S])",
      "description": "'sha256:' + 64 lowercase hex."
    },
    "JudgeV1": {
      "description": "The model that judges, when the evaluator uses one.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "provider",
        "model",
        "config_digest"
      ],
      "properties": {
        "provider": {
          "type": "string",
          "pattern": "^[a-z0-9][a-z0-9_.-]{0,63}(?![\\s\\S])"
        },
        "model": {
          "type": "string",
          "minLength": 1,
          "maxLength": 256,
          "description": "The judge model id as configured. An alias is recorded as given; the model that actually served is observed per trial (ObservedProviderVersionV1)."
        },
        "config_digest": {
          "$ref": "#/definitions/Sha256DigestV1",
          "description": "jcs_v1 SHA-256 of the judge configuration (prompt/rubric template digests, temperature, max tokens, parser settings) -- for a registered judge, EvaluatorVersionV1.judge_config_digest."
        }
      }
    },
    "ObjectiveV1": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name",
        "orientation",
        "weight"
      ],
      "properties": {
        "name": {
          "type": "string",
          "pattern": "^[a-zA-Z_][a-zA-Z0-9_.-]{0,127}(?![\\s\\S])",
          "description": "The repo's canonical metric-name pattern (METRIC_NAME), capped at 128 characters."
        },
        "orientation": {
          "type": "string",
          "enum": [
            "maximize",
            "minimize"
          ]
        },
        "weight": {
          "type": "number",
          "minimum": -9007199254740991,
          "maximum": 9007199254740991
        }
      }
    },
    "EvaluatorVersionManifestV1": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "manifest_version",
        "evaluator_id",
        "code_digest",
        "config_digest",
        "helper_digests",
        "judge",
        "objectives",
        "dependency_versions"
      ],
      "properties": {
        "manifest_version": {
          "type": "integer",
          "const": 1
        },
        "evaluator_id": {
          "$ref": "../common_types_schema.json#/definitions/ForeignKeyId"
        },
        "code_digest": {
          "$ref": "#/definitions/Sha256DigestV1",
          "description": "The fp2 efp2 digest of the evaluator code (docs/fingerprints/fp2.md)."
        },
        "config_digest": {
          "$ref": "#/definitions/Sha256DigestV1",
          "description": "jcs_v1 SHA-256 of the evaluator's BOUND configuration: thresholds, captured parameters, closure-bound values and any other setting that changes a score but is not source text. efp2 covers source only, so without this a model-free evaluator's threshold change would not be a new version. The digest of {} when there is none."
        },
        "helper_digests": {
          "type": "object",
          "maxProperties": 256,
          "propertyNames": {
            "pattern": "^[A-Za-z0-9_.:/-]{1,128}(?![\\s\\S])"
          },
          "additionalProperties": {
            "$ref": "#/definitions/Sha256DigestV1"
          },
          "description": "Contents of LOCAL helper files the evaluator calls (project source, not third-party packages -- dependency_versions covers those), keyed by a stable name. {} asserts none."
        },
        "judge": {
          "oneOf": [
            {
              "$ref": "#/definitions/JudgeV1"
            },
            {
              "type": "null"
            }
          ],
          "description": "The judge model and its configuration; null (explicitly) for a model-free evaluator."
        },
        "objectives": {
          "type": "array",
          "minItems": 1,
          "maxItems": 64,
          "items": {
            "$ref": "#/definitions/ObjectiveV1"
          },
          "description": "The objective set. MUST be sorted ascending by name with unique names (the reference rejects anything else), so one set has one spelling. Changing any objective's orientation or weight is a new evaluator version."
        },
        "dependency_versions": {
          "type": "object",
          "maxProperties": 256,
          "propertyNames": {
            "pattern": "^[A-Za-z0-9_.@/-]{1,128}(?![\\s\\S])"
          },
          "additionalProperties": {
            "type": "string",
            "minLength": 1,
            "maxLength": 64
          },
          "description": "Behaviour-affecting dependency versions (e.g. a scoring library, a tokenizer), package -> exact version. {} asserts none."
        }
      }
    }
  },
  "allOf": [
    {
      "$ref": "#/definitions/EvaluatorVersionManifestV1"
    }
  ]
}
