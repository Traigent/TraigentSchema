{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schemas.traigent.ai/execution/run_identity_binding_v1_schema.json",
  "title": "Run Identity Binding V1",
  "description": "The exact identities and versions one optimization run consumed and produced, under traigent.content_identity.v1: the agent version, the dataset version (dataset_root), the evaluator version and HOW it was resolved, and per trial the multiset of examples actually evaluated (evaluated_root + members) and the provider versions actually observed. This is the record a certificate binds (docs/identity/content-identity-v1.md, 'What certificates bind'). It records CANDIDATES only: applying or promoting a winner is a separate, compare-and-set event on the agent head owned by the Director contract, and is never implied by a run record. Additive; no existing contract changes.",
  "version": "1.0.0",
  "definitions": {
    "EvaluatorVersionBindingV1": {
      "description": "Which evaluator scored the trials. resolution says whether the version was WITNESSED at scoring time (the scorer reported the version it executed) or only DECLARED at session start. A certificate MUST NOT make an evaluator-version claim from a declared-only binding.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "evaluator_id",
        "version_digest",
        "resolution"
      ],
      "properties": {
        "evaluator_id": {
          "$ref": "../common_types_schema.json#/definitions/ForeignKeyId"
        },
        "version_digest": {
          "type": "string",
          "pattern": "^sha256:[0-9a-f]{64}$",
          "description": "EvaluatorVersionV1.judge_config_digest (observability/evaluator_version_schema.json) for a registered judge, or the fp2 efp2 digest for an SDK-local evaluator."
        },
        "resolution": {
          "type": "string",
          "enum": [
            "witnessed_at_scoring",
            "declared_at_session_start"
          ]
        }
      }
    },
    "TrialIdentityV1": {
      "description": "One trial's identity facts. candidate is the agent version this trial's configuration defines (the base build with this trial's config applied).",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "trial_id",
        "evaluated",
        "observed_provider_versions"
      ],
      "properties": {
        "trial_id": {
          "$ref": "../common_types_schema.json#/definitions/ForeignKeyId"
        },
        "candidate": {
          "$ref": "../agents/agent_version_manifest_v1_schema.json#/definitions/AgentVersionV1"
        },
        "evaluated": {
          "$ref": "../datasets/content_identity_v1_schema.json#/definitions/EvaluatedSetV1"
        },
        "observed_provider_versions": {
          "type": "array",
          "maxItems": 256,
          "items": {
            "$ref": "../agents/agent_version_manifest_v1_schema.json#/definitions/ObservedProviderVersionV1"
          },
          "description": "Empty means nothing was observed (for example a mocked run) -- an honest unknown, not 'no providers'."
        }
      }
    },
    "RunIdentityBindingV1": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "scheme",
        "run_id",
        "agent",
        "dataset",
        "evaluator",
        "trials"
      ],
      "properties": {
        "scheme": {
          "$ref": "../datasets/content_identity_v1_schema.json#/definitions/ContentIdentitySchemeV1"
        },
        "run_id": {
          "$ref": "../common_types_schema.json#/definitions/ForeignKeyId"
        },
        "project_id": {
          "$ref": "../common_types_schema.json#/definitions/ForeignKeyId"
        },
        "agent": {
          "$ref": "../agents/agent_version_manifest_v1_schema.json#/definitions/AgentVersionV1",
          "description": "The base agent version the run started from."
        },
        "base_head_generation": {
          "type": "integer",
          "minimum": 0,
          "maximum": 9007199254740991,
          "description": "The agent head generation captured when the run STARTED. A later promotion of any candidate from this run is a compare-and-set against exactly this value (Director contract); recorded here so a certificate can show which head its candidates were measured against."
        },
        "dataset": {
          "$ref": "../datasets/content_identity_v1_schema.json#/definitions/DatasetIdentityV1"
        },
        "search_set": {
          "$ref": "../datasets/content_identity_v1_schema.json#/definitions/DatasetIdentityV1",
          "description": "When the run searched on one split and evaluated on another: the search/selection split's identity. Overlap with the evaluation examples is measured on example_id."
        },
        "evaluator": {
          "$ref": "#/definitions/EvaluatorVersionBindingV1"
        },
        "trials": {
          "type": "array",
          "maxItems": 100000,
          "items": {
            "$ref": "#/definitions/TrialIdentityV1"
          }
        }
      }
    }
  },
  "allOf": [
    {
      "$ref": "#/definitions/RunIdentityBindingV1"
    }
  ]
}
