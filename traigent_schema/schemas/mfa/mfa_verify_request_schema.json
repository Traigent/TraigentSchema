{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schemas.traigent.ai/mfa/mfa_verify_request_schema.json",
  "title": "MFA Verify Request",
  "description": "POST /api/v1/mfa/verify body — completes an MFA-gated login. additionalProperties:true — the backend reads the documented keys and ignores unknown ones (never 422s on extras).",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "token": {
      "type": "string",
      "minLength": 1,
      "maxLength": 2048,
      "description": "MFA verification token / code."
    }
  },
  "required": [
    "token"
  ]
}
