{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schemas.traigent.ai/results/comparison_create_request_schema.json",
  "title": "Optimization Comparison Create Request",
  "description": "POST /api/v1/optimization-comparisons. Modeled on the backend create request (extra=allow / unknown=EXCLUDE → additionalProperties:true; the backend generates the id server-side).",
  "type": "object",
  "required": [
    "run_ids"
  ],
  "additionalProperties": true,
  "properties": {
    "run_ids": {
      "type": "array",
      "minItems": 2,
      "maxItems": 10,
      "items": {
        "type": "string",
        "minLength": 1,
        "description": "A run id."
      },
      "description": "2-10 unique run ids to compare."
    }
  }
}
