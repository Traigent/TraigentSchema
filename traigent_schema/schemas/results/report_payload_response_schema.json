{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schemas.traigent.ai/results/report_payload_response_schema.json",
  "title": "Report Payload Response Schema",
  "description": "Response schema for GET /api/v1/experiment-runs/runs/{run_id}/report-payload. This route returns a bare jsonify(payload) (not wrapped in the success envelope). The payload is generated by ExperimentReportService.generate_report_payload and carries a canonical report_id + schema_version + provenance header. The route also sets X-Report-Data-Hash and X-Report-Schema-Version response headers.",
  "type": "object",
  "properties": {
    "report_id": {
      "type": "string",
      "description": "Unique identifier for this report payload instance",
      "x-identifier": true
    },
    "schema_version": {
      "type": "string",
      "description": "Version of the report payload schema (e.g. '1.0')"
    },
    "provenance": {
      "type": "object",
      "description": "Data lineage and integrity metadata",
      "properties": {
        "data_hash": {
          "type": "string",
          "description": "Hash of the underlying data used to generate this report (also returned in X-Report-Data-Hash header)"
        },
        "run_id": {
          "type": "string",
          "description": "ID of the experiment run this report covers",
          "x-reference": true
        },
        "generated_at": {
          "type": "string",
          "format": "date-time",
          "description": "ISO 8601 timestamp when this payload was generated"
        }
      },
      "required": ["data_hash"],
      "additionalProperties": true
    }
  },
  "required": ["schema_version", "provenance"],
  "additionalProperties": true
}
